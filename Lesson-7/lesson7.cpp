#include "lesson7.h"

// Includes the header and provides the definitions (the actual logic):

int add(int a, int b) {
    return a + b;
}

int Calculator::multiply(int a, int b) {
    return a * b;
}
